def detect_commands_from_prompt(text):
    # Generates a prompt for OpenAI GPT, detailing the commands that can be used and a description of them (and their parameters).
    # The AI is also given the text that the user said and is asked to describe if the user said a command or not.
    pass
